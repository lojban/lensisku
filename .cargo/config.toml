# Cargo configuration
# CXXFLAGS is set here to ensure it's picked up by all cargo commands,
# including 'cargo check'. This is required for dependencies like
# tectonic_xetex_layout which uses ICU headers requiring C++17 features.

[env]
CXXFLAGS = "-std=c++17"

